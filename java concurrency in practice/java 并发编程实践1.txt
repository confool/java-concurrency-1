多线程优势
提高资源利用率，系统吞吐率，界面响应速度，简化代码

举例：

服务器：单线程服务器不能使用阻塞IO
*参考做法：nio获取请求，放到读和写的BlockingQueue，再开n个线程读写

GUI：从主循环处理所有逻辑，变成主线程分发事件，子系统线程接受事件后各自处理逻辑。

**********
多线程问题
安全性（数据被错误更改）
活跃性（饥饿，死锁）
性能（创建线程，上下文切换，锁）